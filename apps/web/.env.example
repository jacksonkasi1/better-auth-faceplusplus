# Public API base URL used by web app in production builds.
VITE_API_BASE_URL=http://localhost:8080

# Face auth mode in web UI:
# true  => ask for email before face scan
# false => face-only flow
VITE_FACEPP_REQUIRE_EMAIL=false
