{
  "name": "better-auth-faceplusplus",
  "private": true,
  "packageManager": "bun@1.2.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "bun run --cwd apps/web dev",
    "dev:server": "bun run --cwd apps/server dev",
    "build": "bun run --cwd packages/db build && bun run --cwd packages/better-auth-face build && bun run --cwd packages/auth build && bun run --cwd apps/server build && bun run --cwd apps/web build",
    "check-types": "bun run --cwd packages/db check-types && bun run --cwd packages/better-auth-face check-types && bun run --cwd packages/auth check-types && bun run --cwd apps/server check-types && bun run --cwd apps/web check-types"
  },
  "devDependencies": {
    "prettier": "^3.8.0",
    "typescript": "^5.9.2"
  }
}
